# limbatus_36 shield hardware requirements (identical to the 34-key build).
#
# R5 of the matrix is wired to NFC1 (nRF52840 P0.09), an NFC antenna pin by
# default. Use P0.09/P0.10 as ordinary GPIO instead so R5 can scan.
CONFIG_NFCT_PINS_AS_GPIOS=y
